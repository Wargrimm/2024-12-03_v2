class images extends Array{
    #endpoint = "/images"
    constructor() {
        super();
    }
    find(params) {
        super.find(params);
    }
    load() {

    }
}
let images = new images();
images.load();